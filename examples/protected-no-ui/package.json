{
  "name": "@emseepea/example-protected-no-ui",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && npm run test:built",
    "test:built": "node --test test/*.test.mjs",
    "test:llm": "npm run build && npm run test:llm:built",
    "test:llm:built": "emseepea-test eval",
    "lint": "oxlint src test eval"
  },
  "dependencies": {
    "@emseepea/server": "0.0.2",
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@emseepea/testing": "0.0.2",
    "@types/node": "24.13.3",
    "typescript": "6.0.3",
    "oxlint": "1.80.0"
  },
  "engines": {
    "node": ">=22"
  }
}
