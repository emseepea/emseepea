# Prepublication Review for Server 0.2.0 and React 0.0.3

Date: 2026-09-05

**Publication is pending.** This record covers the planned `next` releases:

- `@emseepea/server@0.2.0`
- `@emseepea/react@0.0.3`

The npm registry still reports `@emseepea/server@0.1.0` and
`@emseepea/react@0.0.2` on the `next` tag. This record does not show that npm
publication or exact publishing-commit qualification has happened.

## What Changes for Users

TypeScript now rejects undeclared top-level structured-output properties from
regular and streaming tool handlers. Mapped tools already received the same
check. Runtime validation and execution behavior are unchanged.

The React patch updates its dependency on the server package for this release.

## Evidence Available Before Publication

Reviewed source is commit `9bba0da8aed032957730b8443847579524a3cdc4`.
The version pull request produced publishing commit
`ee462b015a998d79666dbe2864f9d88025f5333d`. The final successful publishing
commit will be different because this missing review record stopped the first
publication attempt.

- [Source Quality run 33924672955](https://github.com/emseepea/emseepea/actions/runs/33924672955)
  passed on the reviewed implementation.
- [Publishing-commit Quality run 33926799387](https://github.com/emseepea/emseepea/actions/runs/33926799387)
  passed on the versioned packages.
- [Release run 33927190046](https://github.com/emseepea/emseepea/actions/runs/33927190046)
  passed exact-commit gating and language-model qualification. It stopped
  before publication because this version-specific review record was missing.
- Root type checking, framework build, lint, published-content checks, runtime
  boundary tests, and Changesets status passed locally.
- Compile-time tests cover undeclared properties returned through variables and
  object spreads. They also cover schema transforms, coercion, and defaults
  across public input, backend input, backend output, and public output.
- Independent architecture and Jobs To Be Done reviews passed. Independent
  cognitive-accessibility and Markdown reviews passed for the Changeset.
- Independent pipeline risk review rated commit, push, and release risk at
  5/25, within the project's appetite.

## Required on the Publishing Commit

- Node.js 22 and 24 builds, tests, package checks, OSV scanning, performance
  checks, and load checks.
- Standalone installation, lint, ordinary tests, semantic smoke tests, and UI
  accessibility tests for all eight packed initializer projects.
- All eight examples checked with the pinned Claude CLI and model, using three
  fresh answers and nine independent judgments per case.
- Exact evidence for tool selection, MCP operations, and returned material.
- Independent release-risk review within the project's appetite.

## Required After npm Publication

Both packages must have their planned `next` versions and unchanged `latest`
tags. The workflow must verify provenance, signatures, package checksums, SBOMs,
registry metadata, clean installation, and public imports before creating Git
tags and GitHub releases.

## Evidence Boundary

- This is a compile-time contract improvement. It does not add a runtime
  capability or alter the Model Context Protocol wire behavior.
- Runtime schemas remain the authority at every untrusted boundary.
- The release does not claim full Model Context Protocol profile conformance or
  remove the limits recorded in the protocol coverage guide.
- This is a prepublication review, not npm or GitHub release evidence.

## Review Status

The following result covers this prepublication review only. Exact
publishing-commit qualification, npm publication, provenance, registry readback,
Git tags, and GitHub releases remain pending.

`Result` below refers to this review. `Final result` refers to the release-risk
assessment. The release workflow checks these two exact labels.

- Result: PASS
- Final result: within appetite.
- Publication qualification: pending exact publishing-commit CI.
- Publication completion: pending npm and GitHub verification.
