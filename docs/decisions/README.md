# Decisions Compendium

<!-- AUTO-GENERATED by packages/architect/scripts/generate-decisions-compendium.sh under the decisions-compendium policy — do NOT hand-edit; regenerate via `wr-architect-generate-decisions-compendium`. -->

Compact rendered index of every ADR's chosen option, confirmation criteria, and relationship graph. **Authoritative substance lives in the per-ADR body** (`<NNN>-<slug>.<status>.md`); this compendium is a derived view for routine `wr-architect:agent` compliance review.

**Two sections:**

- **In-force decisions** (`proposed` + `accepted`) — the current rules to follow.
- **Historical decisions** (`superseded` + `rejected` + `deprecated`) — direction for what NOT to do. Useful when reviewing a proposed change that re-treads a path already tried, or that conflicts with a superseded decision's still-valid intent. The status badge on each entry says which kind it is.

For deep-dive — creating, evolving, ratifying, or contesting a decision — open the per-ADR file directly. `/wr-architect:create-adr`, `/wr-architect:capture-adr`, and `/wr-architect:review-decisions` all keep the full body in scope. Decision Drivers, Considered Options bodies, Pros and Cons, Consequences narrative, and Reassessment Criteria are intentionally NOT in this routine view — they live in the per-ADR body.

**Total ADRs:** 94 (55 in-force, 39 historical)

---

## In-force decisions

_55 ADRs. These are the current rules. The architect agent reads this section first for routine compliance review._

### ADR-0006 — Canonical Public Contract and Private Manifest Compilation
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Compile public contracts and private manifests"**.
**Confirmation:** Repeated clean builds produce byte-identical public contract artifacts.; Duplicate, invalid, unresolved, and dependency-incomplete registrations fail startup.; Discovery exactly matches the effective registry through real HTTP.; Public artifacts contain no backend type, credential, destination, or private error detail.; Compatibility checks reject an unclassified breaking public-contract change.

### ADR-0007 — Deterministic Execution Kernel and Checked Boundaries
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"One checked execution kernel"**.
**Confirmation:** Malformed, unauthenticated, unauthorized, and invalid requests cause zero handler and adapter calls.; Backend contract failures cannot emit partial or unvalidated public results.; Two materially different synthetic adapters run without kernel changes.; A bounded workflow obeys the same deadline, cancellation, and error layers as a mapped call.; Public APIs expose no raw response or caller-selected destination escape hatch.

### ADR-0009 — Capability-Scoped Reliability, Effects, and State
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Capability-scoped reliability and state"**.
**Confirmation:** Deadline expiry and disconnect cancellation stop all cooperating downstream work.; Retry tests prove bounds, deadline adherence, and absence on unsafe effects.; Idempotency races create at most the documented number of effects.; Single-instance features are not advertised as multi-instance safe.; Required shared-provider failure removes readiness before accepting dependent work.

### ADR-0010 — POST-Scoped Streaming, Progress, and Subscriptions
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"POST-scoped SSE with bounded state"**.
**Confirmation:** JSON and SSE paths produce the same validated final domain result.; Slow-reader tests prove queue bounds and deterministic overflow termination.; Disconnect, expiry, and cancellation stop downstream work.; Progress cannot appear after a terminal result or error.; Local subscriptions are never described as cross-instance or replayable.

### ADR-0014 — Performance Budget for the Initial JSON HTTP Boundary
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Adopt the guide's provisional JSON/tools budget"**.
**Confirmation:** The benchmark command and environment are committed and reproducible from a clean checkout.; Results separately report throughput, framework CPU, transient allocation, and added bytes.; The baseline passes without disabling validation, limits, security, or telemetry redaction.; Repeated runs include variance and do not hide failed or cancelled requests.; Invalid-input load shows no unbounded allocation, queue, retry, or cardinality growth.; No performance claim is made for an unbudgeted capability path.

### ADR-0023 — Mandatory Cognitive-Accessibility Review for Published Content
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Mandatory specialist review plus a density guard"**, because the realised README failure passed structural checks and required human cognitive-accessibility judgement to identify. Automation remains a regression signal, no...
**Confirmation:** QUALITY.md defines the mandatory review, published-content scope, and; A standing register entry records incomprehensible public content as a risk.; The root README is rewritten into short task-oriented sections and passes a; A runnable automated test rejects an over-dense root README prose paragraph; Every existing published prose surface is inventoried and receives recorded; Changed Changesets, release notes, website prose, and outbound repository prose; No document claims cognitive accessibility or comprehension solely because an

### ADR-0025 — Astro Starlight Documentation Generator
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Astro Starlight"**, because it supplies documentation layouts with little custom code.
**Confirmation:** A clean Node.js 22 and 24 checkout builds the site.; The site uses the existing brand and accessibility requirements.; No custom theme components are introduced without a demonstrated need.

### ADR-0028 — Example-Owned Oxlint with Root Orchestration
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Example-owned Oxlint dependency with root orchestration"**, because an example must carry everything needed to lint itself after it leaves the monorepo.
**Confirmation:** The root and every copyable example declare the same exact MIT-licensed; Every copyable example exposes a lint command that references only paths and; No example lint command references the monorepo root, a root script, or a; Root CI runs one repository-wide lint scan before type checking and tests.; tests/docs/example-quality.test.mjs fails when an example omits lint, uses; At least one example is copied to a temporary directory outside the monorepo;; The future npm init template test creates a project outside the monorepo and; Clean monorepo checkouts pass lint on both supported Node versions.; Intentional suppressions are narrow and state why they exist.

### ADR-0031 — Website Workspace in the Existing Monorepo
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Website workspace in this monorepo"**, because it keeps website tooling separate from published packages.
**Confirmation:** The website has its own package manifest with private: true.; No published package depends on the website workspace.; Website source and examples remain in the same public MIT repository.

### ADR-0032 — Static-Only Website Runtime
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Static files"**, because the initial website does not need application services.
**Confirmation:** Build output can be served as static files without an application process.; The site has no server adapter, API route, runtime secret, analytics, or cookies.; Core guide content and navigation remain usable with JavaScript disabled.

### ADR-0033 — GitHub Pages Website Hosting
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"GitHub Pages"**, because it fits the existing repository workflow.
**Confirmation:** Only checked static output is deployed from the selected revision.; Workflow actions are pinned and permissions are limited to their tasks.; Pull requests cannot access deployment credentials.; Guide checks and the website performance gate pass before publication.

### ADR-0034 — One Source for Reader Guides
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"One guide source"**, because it avoids inconsistent copies.
**Confirmation:** Each guide has one maintained source, with descriptive README links.; Runnable snippets have one source rather than separately edited copies.; Governance records do not displace getting-started guidance.

### ADR-0036 — One Current Documentation Set
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"One current set"**, because multiple supported documentation versions are not yet needed.
**Confirmation:** The initial site has one maintained current guide set.; Pages and runnable guides agree on the supported package versions.; Simultaneous incompatible support triggers reassessment before release.

### ADR-0037 — Local Website Search
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Local search"**, because it avoids adding a hosted search service.
**Confirmation:** Search uses the built site index without hosted search credentials.; Keyboard and screen-reader users can operate search.; Search works for the published guide content.; Index size and browser work are included in website performance measurements.

### ADR-0038 — Measured Website Performance Before Publication
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Measure, then approve a budget"**, because website limits need website evidence.
**Confirmation:** Measurements identify the tested website build and measurement conditions.; All named resource, processing, and memory measurements are recorded.; A separate numerical budget is ratified before public deployment.; The selected publication build passes that budget.

### ADR-0041 — Em See Pea GitHub Organisation Ownership
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** ["ADR-0016"]
**Chosen:** Chosen option: **"Em See Pea organisation ownership"**, because the product now has a dedicated public organisation and the owner has completed the transfer.
**Confirmation:** GitHub identifies emseepea/emseepea as the public authoritative repository.; The organisation and repository use the approved name, mark, description, and website.; GitHub Pages serves the website from https://emseepea.github.io/emseepea/.; Active source, documentation, tests, package manifests, and Changesets use the new repository.; Both public packages trust .github/workflows/release.yml in emseepea/emseepea.; A release from the transferred repository passes exact-revision checks and npm provenance verification.; Existing releases and historical records are not rewritten.

### ADR-0044 — Exact-Commit Trunk Push and Pipeline Watch
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Exact-commit push and watch command"**, because the push and its evidence must be one fail-closed operation. The command non-force pushes the current committed `HEAD` to `emseepea/emseepea` `main`, checks that remote `main...
**Confirmation:** npm run push:watch performs a non-force push of committed HEAD to origin/main.; The command rejects a repository other than emseepea/emseepea.; The remote main SHA must equal the captured local SHA after the push.; Quality and Release runs are selected by workflow identity and exact SHA.; Every matching rerun is watched and must succeed.; Missing, timed-out, cancelled, or failed runs return a nonzero status.; Behavioral tests prove exact-SHA selection, rerun handling, and failure propagation.

### ADR-0047 — Pinned Open Source Vulnerabilities (OSV) Lockfile Scanning
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** ["ADR-0046"]
**Chosen:** Chosen option: **"Pinned OSV lockfile scan"**, because it stops publication when a known vulnerability is found without using npm's advisory endpoint or creating a custom scanner. Both official actions are fixed to one specific commit, and ...
**Confirmation:** Quality scans only the root package-lock.json with OSV Scanner.; Scanner and reporter actions are fixed to specific commits.; The reporter fails the job when the scan reports a vulnerability.; The scan job has a ten-minute timeout.; Standalone initializer qualification requires the scan to pass.; Release retains package signature, integrity, provenance, and clean-install checks.; Quality and Release remain bound to the same exact commit.

### ADR-0048 — Optional Deterministic Filesystem Discovery
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Optional deterministic startup discovery"**, because it removes repetitive registration and catalogue drift while preserving explicit registration and the existing execution and validation boundaries.
**Confirmation:** Repeated discovery returns capabilities in the same order and produces; Effective MCP discovery exactly matches the compiled registry.; Duplicate normalized identities, malformed names, missing declarations,; Development source and built output each work without double registration.; Discovered calls use the existing checked kernel, so invalid external input; Compile-time checks prove that undeclared input access and schema-invalid; Every maintained example and generated initializer uses the convention and; Explicit registration remains supported and filesystem discovery remains

### ADR-0049 — Exact-Commit Release PR Merge and Pipeline Watch
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Exact-commit release pull request merge and watch"**, because the release state change and its fail-closed evidence must be one reproducible operation.
**Confirmation:** npm run release:watch rejects a repository other than; It requires exactly one open changeset-release/main pull request targeting; The merge command includes the captured pull request head revision and uses; The merged pull request supplies one exact merge commit revision.; Quality and Release runs are selected by workflow identity and that exact; Missing, timed-out, cancelled, or failed workflows produce a nonzero exit.; Behavioral tests cover identity validation, exact pull request selection,

### ADR-0050 — Schema-Declared Pass-Through by Default
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Schema-declared pass-through by default with explicit exceptions"**, because it lets compatible backend values evolve without duplicating their catalogue while keeping the public contract deliberate and safe.
**Confirmation:** A test proves that a previously unseen but structurally valid value in an; Malformed backend values and undeclared backend fields cannot reach public; Translation in maintained examples is limited to transport compatibility,; Public and backend schemas remain separately declared and checked.; Framework guidance tells adopters to use direct tools for identity mappings

### ADR-0052 — Optional Deterministic HTTP Route Discovery
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Framework-owned optional HTTP route discovery"**, because it removes repeated route catalogues without copying infrastructure or adding a dependency.
**Confirmation:** Both UI examples and their generated projects register file and page routes; Existing direct Fastify registration continues to work.; Repeated discovery registers routes in the same order.; Tests reject malformed names, non-files, duplicate method and path pairs,; UI browser accessibility, lint, ordinary tests, semantic tests, and packed

### ADR-0057 — Inspectable Semantic Evidence by Default
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** ["ADR-0055"]
**Chosen:** Chosen option: **"Inspectable evidence by default"**, because a semantic test report that hides the response and reason cannot reliably explain its own failure.
**Confirmation:** Evidence contains readable prompts, assistant responses, public tool calls,; A rejected meaning check records all nine configured verdicts before failing.; Tests prove provider and server credential sentinels do not appear anywhere; Tests prove raw provider events, MCP configuration, headers, environment,; Public API guidance warns that evidence contains raw test content and requires; Exact-commit release CI uploads the readable evidence with 14-day retention.

### ADR-0058 — Instance-Agnostic Shared PostgreSQL State
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** ["ADR-0056"]
**Chosen:** Chosen option: **"Instance-agnostic shared state"**, because server identity and retry mechanics are implementation details, while garden bed and harvest date are stable concepts in the user's task.
**Confirmation:** Tool discovery lists only save-harvest-report and get-harvest-report.; Public schemas and responses contain no instance, routing, request ID,; The table primary key is the garden bed and harvest date, with nonnegative; Ordinary tests save through one process, read through another, repeat an; Missing reports return an explicit null result rather than a provider failure.; Tests preserve generic failure redaction, 503 readiness, and bounded database; Semantic tests use natural save and retrieve prompts with exact tool and; The standalone initializer, documentation, package metadata, and release

### ADR-0059 — Process CPU as the Website Work Budget
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** ["ADR-0039"]
**Chosen:** Chosen option: **"Gate total Chromium process CPU"**, because it preserves the broad work limit while avoiding a second, narrower release gate whose isolated spikes cannot be attributed to the website.
**Confirmation:** Every required trial and phase records finite, nonnegative task, script, and; A task-duration value above the former ceiling does not fail publication.; Observed process CPU above 800 milliseconds on desktop or 2,000 milliseconds; Existing file-size, memory, completeness, error, environment, artifact, and

### ADR-0060 — Database Schema Generated Internal Validation
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"PostgreSQL catalog with Kanel and Kanel Zod"**, because it starts from the database contract the example is meant to integrate and uses an existing generator rather than adding project-owned code generation.
**Confirmation:** The initializer creates a private standalone project containing its SQL,; CI creates PostgreSQL from committed SQL, regenerates files, requires a clean; Changing a view shape changes generated TypeScript and Zod output.; Generated files contain no credentials, connection strings, or; Ordinary tests cover read-through-view, write-through-view, stored-procedure,; Semantic tests use the natural MCP journey and demonstrate the view-backed; The package passes the existing standalone, release, provenance, registry,; The README and package description say PostgreSQL demonstrates a transferable; Measured process CPU, memory, and network evidence is recorded before release.

### ADR-0062 — XSD-Generated SOAP Structure and Runtime Validation
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"SOAP client plus shared XSD generation and validation"**, because one XSD graph can precisely generate backend types and govern runtime acceptance without creating a generator inside Em See Pea.
**Confirmation:** The same committed local XSD graph drives TypeScript generation and runtime; Compile-time fixtures using the generated request and response types prove; Namespace identity is preserved in the generated schema model and proven by; Runtime tests accept a positive integer, a 1-to-40-character pea type, zero to; Mapper input and output compile against generated types without casts or; A clean offline generation produces no diff.; Clean generation succeeds with network access disabled and remote WSDL or XSD; Changing an XSD primitive type changes generated TypeScript and runtime; A compatibility test proves the custom transport validates the raw response; Tests cover a successful call, SOAP fault, invalid XML, XSD-invalid XML,; Invalid, malformed, oversized, timed-out, redirected, or externally resolving; Tests independently prove the parser's external-entity and resource limits; OSV, licence, software-bill-of-materials, provenance, and packed standalone; Semantic tests prove natural tool selection and answer meaning without adding; The initializer creates a private standalone project and passes ordinary,; Qualification measures process CPU and memory before release and records

### ADR-0063 — Two MongoDB Collection Validation Patterns
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** [0061-mongodb-json-schema-generated-internal-validation]
**Chosen:** Chosen option: **"Two validation patterns in one initializer"**, because users can compare both boundaries in one small project and choose the approach that matches each existing collection.
**Confirmation:** pea_varieties retains a MongoDB validator equal to its application schema.; pea_observations has no MongoDB validator.; Each collection has exactly one as const standard JSON Schema object used; An invalid variety write is rejected by MongoDB.; An invalid observation presented to the application causes zero MongoDB write; Invalid stored documents from either collection fail application validation; Compatible unseen string values pass through both paths without translation; Variety and observation tools have distinct task semantics and do not expose; Fixed projections, limits, timeouts, generic failures, readiness, and clean; Semantic evaluation covers model-visible variety and observation behavior at; The initializer creates a private standalone project and passes ordinary,; Measured process CPU, memory, and network evidence for both paths replaces the

### ADR-0064 — Typed Authentication with Optional Permission-Shaped Discovery
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** [0018-public-discovery-and-invocation-scoped-oauth-security]
**Chosen:** Chosen option: **"Typed authentication with optional permission-shaped discovery"**, because it keeps the common open-discovery experience simple while supporting teams whose catalogue is itself protected information.
**Confirmation:** With no authentication extension, every maintained initializer starts in; Every maintained initializer documents and tests adding the same protected; Public discovery remains the default when protected capabilities exist.; Default public discovery lists the public contracts for both open and; OAuth protected-resource and authorization-server discovery metadata remain; Explicit protected discovery rejects missing or invalid credentials before; Two principals with different normalized permissions receive the exact; Pagination cursors cannot be reused across different permission views.; A capability hidden from a principal fails closed when called directly.; Hidden and unknown direct calls have the same safe public error and cause zero; Open and protected capabilities coexist in one server.; Tools, resources, templates, and prompts receive identical discovery and; Missing permissions cause zero handler, availability, completion, or backend; Tokens, credentials, raw claims, and provider errors never reach handlers,; Invalid issuer, audience, expiry, intended resource, or permissions cause; The authentication provider is operator-configured, Em See Pea does not act; Outbound-request tests retain fixed destinations, safe parsing, redirect and; Duplicate authentication configuration and unenforceable access policy fail; @emseepea/create-sign-in-tool-server is removed from the canonical package; READMEs, the website, standalone initializer qualification, semantic; Native semantic tests prove, without extra model hints, that principals with; A pinned profile and percentile accompany measured protected-discovery

### ADR-0065 — Typed Operations with Framework-Redacted Observability Adapters
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** [0012-typed-operations-and-opentelemetry-boundary]
**Chosen:** Chosen option: **"Framework-redacted observability adapters"**, because one safe event boundary supports common destinations without giving adapters access to sensitive request or backend data.
**Confirmation:** Every maintained initializer documents and tests optional observability; OpenTelemetry and structured logging adapters consume the same safe event; Two adapters with unique identifiers receive the expected event once and in; Duplicate adapter identifiers fail during startup.; Invalid, missing, and out-of-range safety configuration fails closed.; Liveness and readiness expose no application data and reflect the exact; Tests prove adapters never receive bodies, headers, arguments, results,; Adapter and exporter failures leave protocol status and body unchanged.; Slow or failed flush operations remain bounded and cannot prevent other; Shutdown tests prove admission stops and outstanding work drains or cancels; No public adapter can register lifecycle hooks or bypass authentication,; README, website, API reference, standalone generated-project checks, semantic; Measured CPU, allocation, and event-size evidence meets the existing

### ADR-0066 — Pluggable Detailed Feedback Conversations and Event Hooks
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Optional feedback package with detailed submissions, durable conversations, vendor-authoritative adapters, and typed event hooks"**, because it makes the user-facing and backend contracts reusable without weakening the che...
**Confirmation:** The package exposes separate detailed-submission and conversation constructors; Public submissions accept bounded explanatory prose and application-declared; Conversation tests prove scope isolation, append-only ordering, pagination,; PostgreSQL and Firestore contract tests prove their documented atomicity,; GitHub integration tests prove issue creation, exact issue correlation, support; Zendesk integration tests prove ticket creation, exact ticket correlation,; Checked ingestion tests reject unauthenticated, oversized, invalid, duplicate,; Hook tests prove immutable minimal events, bounded dispatch attempts, failure; Native multi-turn semantic tests, without coaching context, prove the AI; Semantic evidence contains synthetic non-sensitive conversations, tool calls,; Ordinary checks run before the later, more expensive semantic evaluations.; README files, the website, API reference, generated standalone projects,; No feedback performance claim is published until representative database and

### ADR-0067 — Capability-Local Static Discovery Suppression
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Capability-local static discovery flag"**, because it is the smallest public API, keeps retirement intent with the definition, and avoids repeating capability identities in deployment composition.
**Confirmation:** Omitting discoverable and setting it to true produce the current catalogue; Setting discoverable: false omits the exact entry from tools/list,; Completion for a known hidden prompt or resource template remains available; Public and protected lifecycle-hidden capabilities remain subject to their; Protected discovery first removes lifecycle-hidden entries and then applies; A category containing only lifecycle-hidden capabilities remains advertised; Tools, static resources, resource templates, and prompts have ordinary tests; Explicit arrays and deterministic filesystem discovery produce identical; Paginated discovery never includes hidden entries, and its stable cursors are; Startup still rejects duplicate or invalid registrations independently of; An end-to-end compatibility check proves the sequence visible and callable,; Published documentation states that discovery suppression is a retirement

### ADR-0068 — Protected POST Progress Behind a Trusted Proxy
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** ["ADR-0030"]
**Chosen:** Chosen option: **"Allow protected POST progress after framework authentication"**.
**Confirmation:** Invalid proxy, forwarding, HTTPS, authority, origin, request-size, method,; Missing, invalid, wrong-resource, expired, or insufficient credentials return; Authentication and authorization finish before SSE headers or events begin.; Valid protected calls receive strictly ordered bounded progress and exactly one; Concurrent calls routed to two server processes never exchange principals,; Tests fail on a wrong, missing, duplicate, oversized, or late event.; Slow readers, operation and verifier deadlines, disconnect, cancellation, and; Load qualification runs in pull-request and exact-release CI on Node.js 22 and; The official MCP client and an independent raw HTTP client both complete the; Public documentation claims only protected POST-scoped progress and explicitly

### ADR-0069 — Atomic Runtime Activation of Startup-Compiled Capabilities
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Atomic activation of startup-compiled capabilities"**, because it supports uninterrupted catalogue changes without moving schema, handler, or access-policy compilation into the running request boundary.
**Confirmation:** Omitting runtimeCatalogueUpdates preserves current behaviour and public; The update function accepts only a complete active set drawn from the checked; Valid activation and deactivation cover tools, static resources, resource; Duplicate, foreign, or invalid proposed entries throw and leave the previous; A test pauses an authenticated request, publishes a replacement, and proves; Inactive and permission-hidden direct calls return the same safe unknown; Active lifecycle-hidden capabilities remain absent from list discovery but; Public and protected discovery apply active selection, lifecycle suppression,; Old-revision, malformed, cross-method, and cross-permission pagination cursors; Runtime update mode rejects positive reuse lifetimes for discovery, all catalogue; Server discovery continues to report listChanged: false; documentation says; Existing page-size, page-byte, request, result, timeout, cancellation,; Ordinary tests cover the exact boundaries. A released-package journey proves

### ADR-0071 — Separate OpenAPI-Backed Example and Initializer
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** [0070-openapi-generated-backend-types-and-runtime-validation]
**Chosen:** Chosen option: **"Separate OpenAPI-backed example and initializer"**, because the spec-driven and no-spec journeys begin from materially different developer inputs even though they share the same checked runtime boundary.
**Confirmation:** npm init @emseepea/openapi-backed-server -- <directory> creates the new; The existing API-backed initializer retains hand-written backend schemas and; The OpenAPI-backed project contains one canonical OpenAPI 3 contract, selects; Clean offline regeneration produces no diff.; The generation command uses typed-openapi@4.0.1 with Zod 4, strict; A committed Swagger 2 fixture upgrades through; Local fragment $ref values are accepted; URL and relative-file references; Changing a required field, optional field, primitive category, or supported; Invalid mapped backend requests cause zero HTTP calls, and invalid provider; Callers cannot select the backend origin, operation, credentials, redirects,; Public MCP schemas remain separate and prevent undeclared provider fields or; Both examples and generated projects independently install, lint, build, run; The new initializer passes accessibility, licence, lockfile vulnerability,; The canonical public-package list and template comparison documentation

### ADR-0072 — Opt-In Integrity-Protected Request State
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Opt-in signed request state using the MCP SDK"**.
**Confirmation:** With no request-state setting, current stateless behavior and public types do; Configured direct tools, static resources, resource templates, and prompts can; The implementation uses the installed MCP SDK codec rather than custom; Keys shorter than 32 bytes and invalid lifetimes or size limits fail at; Tampered, expired, oversized, wrong-method, wrong-capability, wrong-principal,; Protected handlers authenticate and authorize every round.; Handlers receive verified decoded state and examples validate its application; Tests cover restart and multi-process use with the same configured key.; Official-client and raw-HTTP tests pass from a packed clean install.; Documentation states that signing is not encryption or replay prevention and

### ADR-0074 — Same-Endpoint Stateless Legacy Protocol Support
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** [0005-active-streamable-http-scope-and-adaptive-delivery]
**Chosen:** Chosen option: **"Same-endpoint stateless legacy fallback"**, because it provides broad interoperability through the SDK's existing compatibility path without introducing a second endpoint, dependency, capability registry, or execution kern...
**Confirmation:** An independent legacy client completes initialize, tools/list, and; Existing independent MCP 2026-07-28 discovery, listing, and invocation; Correctly classified legacy requests bypass only modern-era envelope and; Malformed, mixed-era, and unsupported-version requests fail closed and cause; Tests prove that each legacy request uses a fresh server instance and retains; GET, DELETE, PUT, PATCH, and OPTIONS /mcp remain 405 Method Not; No session identifier, session store, replay, resumption, or GET stream is; Modern-only and unsupported legacy capabilities are not advertised on legacy; The protocol coverage ledger distinguishes the MCP 2026-07-28 surface from; The existing JSON HTTP performance budget passes for the expanded request

### ADR-0075 — Opt-In Bounded Request-Scoped MCP Logging
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Opt-in bounded request-scoped logging using the MCP SDK"**, because it implements the protocol feature without introducing connection state or an unbounded output path.
**Confirmation:** Without logging configuration, discovery, handler types, and JSON response; With logging configured, modern discovery advertises logging; legacy; Only the named direct handler contexts expose reportLog.; A pinned official client receives only requested messages at or above its; A request without io.modelcontextprotocol/logLevel receives no log message.; Invalid levels, logger text, non-JSON data, oversized messages, too many; Authentication and authorization failures produce no stream and make no; Concurrent requests cannot receive each other's messages.; logging/setLevel is rejected before application work for modern and legacy; Framework observability still receives only its existing redacted event and; Raw HTTP, official-client, packed-package, slow-reader, disconnect, and; Documentation calls this channel deprecated and distinguishes it from server

### ADR-0076 — npm Scripts as the User-Facing Command Contract
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"npm scripts are the user-facing command contract"**, because npm already provides the smallest common command surface across every example and generated project.
**Confirmation:** Every generated initializer manifest contains exactly; Routine public documentation calls npm run container:build and contains no; Database examples expose db:start and db:reset; their dev scripts compose; Documentation describes db:reset as deleting the local database volume; Existing build, start, test, lint, and generation instructions use their; Automated documentation checks fail when a routine public instruction bypasses; No second task runner or speculative script is added.

### ADR-0077 — Digest-Pinned Official Node Builder and Distroless Runtime
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Official Node builder with a Google Distroless runtime"**, because build tooling belongs in the build stage while the production stage needs only a minimal Node runtime whose exact contents, signature, user, and architectu...
**Confirmation:** Every example Dockerfile uses identical digest-pinned official Node; Both images report the exact Node 24 patch in .node-version.; Both OCI indexes contain linux/amd64 and linux/arm64/v8 manifests.; The Distroless signature matches Google's documented keyless issuer and; The release image reports a non-root user and contains no shell or package; Every generated image passes health, readiness, runtime configuration, secret; Native runtime dependencies pass on both required architectures.; A base refresh rebuilds and qualifies every standalone initializer image.; Release evidence records both exact base references used by the qualified; No automatic-refresh claim is made unless updater configuration and its checks

### ADR-0078 — Example-Owned Production Containers Behind Trusted Proxies
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Example-owned production containers behind trusted proxies"**, because the copyable examples should teach the complete safe deployment path, not merely produce an image that cannot be deployed or is unsafe when exposed.
**Confirmation:** The canonical initializer list requires all eleven examples, and no; Every initializer passes its documented install, npm image build, hardened; Prepublication qualification uses exact packed first-party artifacts and fails; Image inspection satisfies every ADR-0077 base, signature, architecture,; Image history and context contain no secrets, credential files, local; Missing or invalid production policy exits before listening; local startup; Every journey rejects invalid forwarding, authority, origin, proxy address,; Database Compose ports remain loopback-bound and its credentials and purpose; Ordinary JSON performance reports the exact profile, four request classes,; Protected progress reports only ADR-0068's load and safety evidence.; Website, README, npm-script, link, build, and cognitive-accessibility checks

### ADR-0079 — Opt-In Checked Client Roots
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Checked bounded roots through existing input-required rounds"**, because it completes the protocol behaviour through the existing round-trip boundary without letting unchecked client-controlled paths reach application code...
**Confirmation:** Omitting clientRoots preserves current public types and runtime behaviour; Invalid clientRoots values fail server creation. The default accepts at most; Direct public and protected tools, static resources, resource templates, and; Raw HTTP tests cover a valid request and continuation, missing or malformed; Protected tests prove that every round authenticates and authorizes normally; Signed request-state tests prove restart and cross-process continuation without; Mapped tools, streaming tools, completions, lists, subscriptions, legacy; Type tests cover the roots helper and accessor and reject raw or wrong-kind; Packed-package and published documentation checks exercise the released API.

### ADR-0080 — Immutable Capability Catalogues Through Redeployment
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Immutable catalogues through redeployment"**, because Em See Pea has no demonstrated need to mutate a checked capability catalogue inside a running process.
**Confirmation:** Capability catalogues are compiled once at startup and cannot be replaced through a public or MCP API.; Modern discovery advertises listChanged: false; no tools, resources, or prompts list-change notifications are ...; Two separately started deployments with different checked definitions expose their respective catalogues only ...; The old process retains its original catalogue until shutdown.; Rolling deployments may temporarily expose different catalogues across processes.; Existing subscriptions end with their process; no reconnect, replay, or recovery guarantee is introduced.; Raw HTTP, official-client, protected-discovery, pagination, legacy, packed-package, documentation, and release...; Documentation states that catalogue changes require redeployment and fresh MCP initialization.

### ADR-0081 — Application-Owned Model Provider Integration Instead of Deprecated MCP Sampling
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Application-owned model-provider integration without MCP Sampling"**, because MCP 2026-07-28 tells new implementations not to adopt the deprecated feature and no demonstrated Em See Pea compatibility need outweighs that di...
**Confirmation:** Public framework types expose no Sampling request helper, response accessor, or accepted input-request type.; A hand-built sampling/createMessage input request is rejected before delivery or continuation work.; Modern and legacy discovery remain unchanged and advertise no Sampling support.; Protocol coverage and published framework guidance identify Sampling as intentionally unsupported and recommen...; Source, type, black-box, packed-package, and released-package checks preserve the same rejection boundary.; Registry verification confirms that the released package exposes neither Sampling types nor runtime behavior.

### ADR-0082 — Checked Protocol-Native Tool Results
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Checked protocol-native results plus structured convenience"**, because it preserves Em See Pea's safe framework boundary without narrowing the responses that protocol-compliant applications can return.
**Confirmation:** Type checks accept the existing { data, text? } result only when an outputSchema is declared.; Type checks accept protocol-native results with all five content-block variants, isError, _meta, and every JSO...; Type and runtime checks reject mixed convenience and protocol-native forms.; Successful protocol-native results with an outputSchema require conforming structuredContent.; Error results with isError: true do not require structured content, even when an output schema exists.; Tools without an outputSchema can return content-only, deliberate-error, and safely checked structured results...; Unsafe JSON, malformed blocks, unknown framework-owned metadata, and oversized results produce the existing ge...; Application isError: true reaches the client, while thrown exceptions, cancellation, and deadlines retain gene...; Resource links and embedded resources grant no registration, dereference, or authorization authority.; Direct, mapped, and streaming tools use the same checked result path.; Existing convenience handlers preserve their explicit-text and generated-text behavior.; Modern and legacy protocol journeys preserve their documented behavior.; Packed-package and released-package journeys exercise both result forms.; Registry readback verifies the released package and public types separately from source and CI evidence.

### ADR-0084 — Parallel React and Svelte Result Cards with Measured Bundle Guidance
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** [0083-canonical-accessible-tool-result-views-and-mcp-apps-lifecycle]
**Chosen:** Chosen option: **"Parallel React and Svelte result cards with measured guidance"**, because it preserves a normal React integration for existing adopters while testing whether a compiled Svelte implementation materially reduces the cost of ...
**Confirmation:** React and Svelte export ResultCard against the same parsed ResultView.; Both renderers preserve the native semantic, ARIA, keyboard, focus, hostile; Both lifecycle bindings use one dependency-free checked MCP Apps controller; A checked script builds equivalent production fixtures and reports raw,; Documentation applies the 20% dual-compression threshold and scopes any; React and Svelte remain absent from the server package's required dependency; Both public packages pass pack, fresh-install, provenance, and registry; The cited React adopter replaces its generic result presentation and host

### ADR-0085 — Checked Multi-Content Resource Read Results
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Checked multi-content resource reads"**, because Model Context Protocol (MCP) allows one resource read to return several content items. Em See Pea will still treat the originally requested resource as the only authorized r...
**Confirmation:** Static-resource and resource-template handlers can each return multiple text; The official MCP client pinned to 2026-07-28 receives every content intact.; Legacy clients preserve their documented resource-read behavior.; Protected reads authenticate and authorize the requested capability before; A returned URI gains no listing entry or direct-read authority.; Results invalid under the software development kit (SDK) schema and oversized; Cancellation, deadlines, cache instructions, discovery suppression, and; Source, type, black-box, packed-package, and benchmark checks pass from clean; A released-package journey exercises both static and template multi-content; Registry readback verifies the released package version, integrity,; Protocol coverage and package guidance document the authority and cache

### ADR-0086 — Application-Owned Styling with Public Host-Aware Theme Resolution
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** [0011-framework-neutral-accessible-elicitation-and-approval-ui]
**Chosen:** Chosen option: **"Public `useMcpTheme` resolver hook"**, because it centralises the repeated reactive and server-rendering-safe browser boundary while keeping the existing MCP Apps connection and application styling boundaries separate.
**Confirmation:** Package tests prove the host theme wins over either system preference.; Browser tests prove an absent host theme follows system preference changes.; Tests prove preference listeners are removed on cleanup.; Server rendering completes without a window global and returns "light".; Documentation shows an application applying the returned value without; The hook and its dependencies remain confined to @emseepea/react.; Existing MCP Apps lifecycle, renderer, accessibility, pack, and fresh-install

### ADR-0087 — Bounded Request-Scoped Progress for Resources and Prompts
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Bounded progress for resources and prompts"**, because it closes the smallest independent non-deprecated protocol gap by reusing an existing request-scoped mechanism.
**Confirmation:** Static-resource, resource-template, and prompt handlers can report progress; Requests without a progress token expose no reporter and send no progress; Progress notifications use only the current request's token and arrive before; Every fresh input-required round is isolated from earlier progress tokens.; Authentication and authorization occur before protected handlers run.; Existing cancellation, deadline, result-validation, event-count,; Direct and mapped tool contexts, completion handlers, notification POST; Source, type, black-box, packed-package, and benchmark checks pass from clean; A released-package journey independently verifies resource and prompt; Registry readback independently verifies the released package version,; Protocol coverage and package guidance describe the new optional behavior and

### ADR-0089 — No `ping` in Model Context Protocol (MCP) `2026-07-28` Beyond Existing Legacy Compatibility
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** [0088-always-available-checked-mcp-ping]
**Chosen:** Chosen option: **"Keep MCP `2026-07-28` `ping` absent and preserve legacy behavior"**, because exact protocol support is safer than inferring support from generic cross-version package exports.
**Confirmation:** Raw MCP 2026-07-28 ping receives method-not-found.; Two independent official clients pinned to MCP 2026-07-28 observe the same; Every supported 2025-era revision retains its existing official SDK ping; No application handler or authentication verifier runs for rejected MCP; No progress, logging, cache, state, capability, or public API is added.; Client logging, roots, sampling, tasks, sessions, replay, reconnect, and; The pinned dependency's version-specific request registries are checked; Existing source, black-box, packed-package, and released-package behavior; Protocol coverage states that ping is absent from MCP 2026-07-28 and remains; ADR-0088 is retained as superseded history rather than rewritten.

### ADR-0090 — Deprecated Client Logging Kept Out of Adoption Guides
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Remove client-logging adoption guidance while keeping factual coverage"**, because it stops directing new applications toward deprecated client-visible logging without hiding compatibility limits or changing runtime behavi...
**Confirmation:** Search the root README, package and example READMEs, and website guides for; These adoption surfaces contain no client-logging tutorials, copyable; docs/protocol-coverage.md still states the exact client-logging; The client-roots compatibility guide and Sampling alternative remain.; ADR-0075 and client-logging changelogs remain as historical and compatibility; Runtime source, public API declarations, and protocol tests are unchanged.; Documentation, cognitive-accessibility, website-build, and website-size gates; The published website is fetched independently and contains no removed

### ADR-0091 — Shared Result Card Styles in the Existing Optional Stylesheet
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"Extend the existing optional stylesheet"**, because the existing public stylesheet already owns the checked optional presentation baseline and one import avoids copied CSS, selector mapping, and a second package entry poin...
**Confirmation:** The built stylesheet must target both view and result-view without changing; Native, React, and Svelte must render the same Result Card parts and receive the; Renderers must not import CSS, and non-UI servers must acquire no React, Svelte,; With the stylesheet absent, names, roles, states, order, focus, status; Light and dark text, surface, border, error, state, and focus colour pairs; Keyboard, focus, text-spacing, forced-colours, reduced-motion, 320-pixel; Metrics, labelled lists, disclosure, actions, status, hints, and disclaimers; The existing 10 KiB raw and 3 KiB gzip stylesheet budgets must continue to pass.; Narrow product UI guidance must record the accepted Result Card style contract;; The canonical Result Card guide must document the optional import, theme; Exact-commit source and CI, packed-package, anonymous exact-version registry,; A package may be called PUBLISHED only after release and registry evidence;

### ADR-0094 — Checked Dual MIME Compatibility for MCP App Resources
**Status:** proposed | **Oversight:** confirmed | **Supersedes:** ["ADR-0093"]
**Chosen:** Chosen option: **"Checked two-value MIME selection"**, because it preserves the modern contract by default while allowing the one evidenced legacy contract to adopt the existing packaging boundary without a breaking MIME change.
**Confirmation:** Public type checks accept an omitted mimeType,; Definition-time runtime checks reject unsupported MIME values received across; Black-box modern and legacy MCP client checks observe; Equivalent black-box checks observe text/html+skybridge in both operations; Existing URI, metadata, Content Security Policy, access, document,; A migration or published-contract check for the evidenced adopter preserves; ADR-0094 must be ratified before implementation begins. Until then, ADR-0093

### ADR-0095 — Opt-In Check Policy Modules for Adopter Contracts
**Status:** proposed | **Oversight:** confirmed
**Chosen:** Chosen option: **"One opt-in check policy module"**, because one structured boundary removes the duplicated command lifecycle while leaving each adopter's compatibility meaning in its own executable code.
**Confirmation:** Running check without --policy preserves the existing accepted inputs,; --policy is accepted only by check, resolves one local module, and requires; A focused policy fixture receives an extracted current contract and ordered; An empty structured result returns exit 0. Valid break records use the; Import failure, a missing export, a thrown error, or any malformed result is; Token-sentinel tests prove the command does not pass the token to the policy; Capture behavior and baseline files remain unchanged; no migration result is; Public documentation keeps baseline retention, approvals, capture timing,; The evidenced adopter can replace its command-lifecycle wrapper while keeping; Source and package checks confirm the module contract separately from npm; This decision must be ratified before implementation begins.
### ADR-0096 — Open-by-Default Published Output Schemas
**Status:** proposed | **Oversight:** confirmed
**Decides:** Published output schemas permit unknown fields by default, with closed contracts available by explicit declaration, because a published output schema states what a client must tolerate rather than what this server version sends — and open content is what keeps JSON interfaces evolvable. The author-side contract stays closed, input and output publication are brought into agreement, and the resulting one-time comparator break is classified as a deliberate migration requiring one baseline re-capture per adopter.
**Confirmation:** Plain object output schema publishes permissive `additionalProperties`; input schema also permissive so both directions agree; handler returning an undeclared key is rejected and the response carries only declared fields; explicitly closed output contract still publishes closed; pre-change baseline compared against post-change schema reports the closed-to-open change as a migration; release notes state the break and the re-capture step.
**Related:** ADR-0095

---

## Historical decisions

_39 ADRs. These were tried and superseded, rejected, or deprecated. Read them as direction for what NOT to do, or to understand the lineage of an in-force decision. Do not enforce them as current rules._

### ADR-0001 — Public-Specification-First TypeScript Framework Foundation
**Status:** superseded | **Oversight:** confirmed
**Superseded-by:** ADR-0004
**Chosen:** Chosen option: **"Public-specification-first framework using the official SDK"**.
**Confirmation:** Clean installs, builds, and tests pass on Node.js 22 and 24.; Raw HTTP tests cover discovery, listing, calling, malformed input, header mismatch, and disabled capabilities.; An independent MCP client pinned to 2026-07-28 interoperates through the real HTTP endpoint.; Every example imports only the public framework API for MCP behavior.; Capability advertisements are derived from enabled handlers and match tested behavior.; UI qualification includes semantic structure, keyboard operation, visible focus, accessible names, status anno...

### ADR-0002 — Explicit Anonymous Production Boundary Behind a Trusted Proxy
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Explicit trusted-proxy production profile"**, because it is the smallest production boundary whose trust and resource assumptions can be stated and tested honestly.
**Confirmation:** public binding is impossible in the loopback profile;; untrusted peers and IPv4-mapped comparison errors are rejected;; missing, repeated, chained, or non-HTTPS forwarding metadata is rejected;; invalid client IPs, authorities, ports, and origins are rejected;; rate limits, bounded state, saturation, and window expiry behave as configured;; accepted and rejected requests are measured before any performance claim;; health and readiness remain free of application data and locally usable;; the original loopback behavior remains qualified; and; no authentication, protected, streaming, distributed, or performance

### ADR-0003 — Public Windy Road Repository with Gated Changesets Releases
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Public repository with private packages and gated Changesets release PRs"**, because it makes development and quality evidence public while preserving an explicit later decision for registry identity and publication.
**Confirmation:** GitHub identifies windyroad/emseepea as public with main as its default branch.; The repository includes the exact MIT licence text and public contribution, security, and support boundaries.; Clean GitHub-hosted checkouts pass quality jobs on Node.js 22 and 24.; Workflow dependencies use immutable pins and least-privilege permissions.; The Changesets workflow creates or updates a release PR on main.; No workflow can publish to npm while packages are private, no publish command is configured, and no registry c...; Required checks protect main before a publishing path is ever enabled.

### ADR-0004 — Fastify-First TypeScript Framework Foundation
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0001"]
**Chosen:** Chosen option: **"Fastify-first official MCP integration"**.
**Confirmation:** Clean installs, builds, and tests pass on Node.js 22 and 24.; Real Fastify HTTP tests cover discovery, listing, calling, malformed input, and disabled capabilities.; An independent MCP client interoperates through the Fastify endpoint.; Examples use only the public Em See Pea API for MCP behavior.; No public generic request-handler, server adapter, or caller-supplied capability map exists.

### ADR-0005 — Active Streamable HTTP Scope and Adaptive Delivery
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Active surface with adaptive delivery"**.
**Confirmation:** Every release names only its currently proven modules and deployment boundary.; The coverage ledger traces every shipped claim to public requirements and passing evidence.; Disabled capabilities are absent from discovery and safely rejected.; Delivery may be reordered without violating recorded dependency gates.; No deprecated, stdio, GET stream, session, replay, or extension claim appears implicitly.

### ADR-0008 — Public and OAuth Protected Resource Security
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Explicit public or OAuth-protected operations"**.
**Confirmation:** Missing access declarations fail startup.; Invalid issuer, audience, expiry, scope, object, or tenant authorization causes zero application calls.; Caller tokens never appear in backend authorization or logs.; SSRF tests cover parsing, redirects, DNS/address changes, prohibited address ranges, and size limits.; Public operations do not accidentally inherit protected credentials or identity.

### ADR-0011 — Framework-Neutral Accessible Elicitation and Approval UI
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Optional React renderer and Tailwind style packages"**.
**Confirmation:** Native and React/Tailwind examples render the same shared fixtures and states.; The React/Tailwind example imports @emseepea/react, not private source.; The example uses exactly one @emseepea/tailwind stylesheet import.; The example has zero Tailwind config, plugin, theme CSS, or selector mapping.; Tailwind dependencies exist only in the Tailwind package's development closure.; Security and approval negative-flow tests pass.; Client presentation cannot authorize effects.; Keyboard and screen-reader users complete every interaction state.; Automated WCAG 2.2 AA and hydration checks pass.; Manual keyboard and screen-reader evidence accompanies every official renderer release.; Document title, language, skip link, main landmark, H1, heading, image, SVG, and icon rules pass.; Core installation and no-UI examples install without React, React DOM, or Tailwind.; Both optional packages install, build, test, and pass accessibility checks independently.; Embedded package renderers do not create a title, language, main, H1, or fixed heading level.; Shared native and React fixtures pass with and without the published stylesheet.; Focus, contrast, forced-colors, reduced-motion, reflow, and target-size checks pass.

### ADR-0012 — Typed Operations and OpenTelemetry Boundary
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Typed configuration and isolated OpenTelemetry boundary"**.
**Confirmation:** Invalid, missing, and out-of-range safety configuration fails closed.; Liveness and readiness expose no application data and reflect dependency failure.; Redaction tests cover tokens, secrets, inputs, URLs, and backend errors.; Exporter failure leaves protocol status and body unchanged.; Shutdown tests prove admission stops and work ends within the configured deadline.

### ADR-0015 — Ordinary Evidence and Exact Release Claims
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Ordinary reproducible evidence and exact claims"**.
**Confirmation:** Every public claim maps to current tests and named clean-checkout artifacts.; Raw HTTP negative tests prove invalid and security failures cause zero application calls.; Independent clients and synthetic services exercise only the public package boundary.; Checksums, SBOMs, locked dependencies, and independent reviews accompany release evidence.; No custom certification key, challenge, trust-root, attestation, or report-validation subsystem exists.

### ADR-0016 — Em See Pea Product npm Scope with Server-Named Runtime
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0004"]
**Chosen:** Chosen option: **"Product scope with role-specific package names"**, because `@emseepea/server` is concise, groups the product family under its own public npm identity, and accurately signals the ratified Fastify-first server foundation.
**Confirmation:** Workspace manifests and imports use @emseepea/server and no longer use; Optional UI documentation uses @emseepea/react and @emseepea/tailwind.; Private example names follow @emseepea/example-* and remain ignored by Changesets.; The runtime depends on Fastify 5 and @modelcontextprotocol/fastify@2.0.0.; Clean installs, builds, and tests pass on Node.js 22 and 24.; Real Fastify HTTP tests cover discovery, tool listing, tool calls, malformed; An independent MCP client interoperates through the Fastify endpoint.; Examples use only the public Em See Pea API for MCP behavior.; No public generic request handler, server adapter, or caller-supplied; The public repository remains windyroad/emseepea under MIT.; Every package and the root remain private, and no workflow can publish to npm.; npm publication requires a later reviewed decision and trusted-publishing setup.

### ADR-0017 — Public Discovery with Protected Tool Invocation
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0008"]
**Chosen:** Chosen option: **"Public discovery and listing, invocation-scoped protection"**.
**Confirmation:** A client without a token can call server/discover and tools/list.; Protected tools are listed with only public contract and access metadata.; Missing, invalid, expired, wrong-audience, or insufficient-scope tokens cause; Public tools still execute without credentials when protected tools coexist.; Caller tokens never appear in backend authorization, public output, or logs.; Disabled capabilities remain absent and no protected-path performance claim

### ADR-0018 — Public Discovery and Invocation-Scoped OAuth Security
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0017"]
**Chosen:** Chosen option: **"Public discovery with invocation-scoped OAuth and retained outbound controls"**.
**Confirmation:** A client without a token can call server/discover and tools/list.; Protected tools are listed with only public contract and access metadata.; Missing, invalid, expired, wrong-audience, or insufficient-scope tokens cause; Public tools still execute without credentials when protected tools coexist.; Caller tokens never appear in backend authorization, public output, or logs.; SSRF tests cover URL parsing, redirects, DNS and address changes, prohibited; Public operations never inherit protected credentials or identity.; No protected-path performance claim is made without a separate budget.

### ADR-0019 — Public Pre-Alpha Releases Through npm Trusted Publishing
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0003"]
**Chosen:** Chosen option: **"Public pre-alpha releases through npm trusted publishing"**, because it proves the real adopter path while keeping release authority short lived, workflow-specific, and independently verifiable.
**Confirmation:** @emseepea/server is public under MIT; the root and examples remain private.; The first publication is exactly @emseepea/server@0.0.1 under next.; Changesets creates the release pull request, required checks pass before; The workflow publishes only through next, and anonymous registry inspection; The exact publishing commit passes build, tests, audit, performance, and pack; npm records GitHub Actions trusted-publisher provenance for the public package.; No NPM_TOKEN or other durable npm write secret is present after bootstrap.; The GitHub release contains the exact commit, supported slice and exclusions,; Anonymous npm view, exact-version 0.0.1 clean install, public import, and; The corresponding GitHub tag and release exist only after npm publication.; SECURITY.md, SUPPORT.md, and CONTRIBUTING.md state the public boundaries.

### ADR-0020 — Mandatory Semantic LLM Qualification for Examples and Releases
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Mandatory Promptfoo qualification with GitHub Copilot CLI"**, because it tests the user-visible meaning of every example while retaining a bounded, auditable, exact-commit release gate.
**Confirmation:** Every example has at least one Promptfoo case with deterministic critical facts,; Every case runs three uncached agent trials and three judge verdicts, and all; Tool trials record the named MCP server and tool calls; resource and prompt; Agent trials use at most three MCP tool calls and the configured response-credit,; CI proves the effective GitHub Copilot CLI provider and; The eval job has only contents: read and copilot-requests: write; no model; Promptfoo and Copilot CLI are pinned development dependencies and are absent; npm test and npm run test:eval are distinct commands and gates.; Redacted JSON evidence contains the required exact-commit and execution fields,; The release job depends on a passing semantic evaluation of the exact publishing; Public claims remain specific to the tested provider, model, cases, and SHA.

### ADR-0021 — Mandatory Semantic LLM Qualification for Examples and Releases
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0020"]
**Chosen:** Chosen option: **"Mandatory Promptfoo qualification with GitHub Copilot CLI"**, because it tests the user-visible meaning of every example while retaining a bounded, auditable, exact-commit release gate.
**Confirmation:** Every example has Promptfoo qualification with deterministic critical facts,; Every example runs three uncached agent trials and three judge verdicts, and all; Tool trials record the named MCP server and tool calls; resource and prompt; Agent trials use at most three MCP tool calls and the configured response-credit,; CI proves the effective GitHub Copilot CLI provider and; The eval process allows only the named loopback MCP server and required tools;; The eval job has only contents: read and copilot-requests: write; no model; Promptfoo and Copilot CLI are pinned development dependencies and are absent; npm test and npm run test:eval are distinct commands and gates.; Redacted JSON evidence contains the required exact-commit and execution fields,; The release job depends on passing semantic evaluation of the exact publishing; Public claims remain specific to the tested provider, model, examples, and SHA.

### ADR-0022 — Harness-Mediated Semantic LLM Qualification for Examples and Releases
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0021"]
**Chosen:** Chosen option: **"Harness-mediated MCP material with GitHub Copilot CLI"**, because it tests the user-visible meaning of every example while retaining a bounded, auditable, exact-commit release gate and working within provider policy.
**Confirmation:** Every example has Promptfoo qualification with deterministic critical facts,; Every example runs three uncached agent trials and three judge verdicts, and all; Every trial records the exact official-client operation and bound material digest.; Agents and judges have no MCP access and use the configured response-credit,; CI proves the effective GitHub Copilot CLI provider and; The harness alone connects to the named loopback MCP server; model processes have no MCP tools;; The eval job has only contents: read and copilot-requests: write; no model; Promptfoo and Copilot CLI are pinned development dependencies and are absent; npm test and npm run test:eval are distinct commands and gates.; Redacted JSON evidence contains the required exact-commit and execution fields,; The release job depends on passing semantic evaluation of the exact publishing; Public claims remain specific to the tested provider, model, examples, and SHA.

### ADR-0024 — Subscription-Backed Claude Semantic Release Checks
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0022"]
**Chosen:** Chosen option: **"Pinned Claude CLI with subscription OAuth"**, because it preserves the existing purpose-built Promptfoo harness, works in GitHub Actions with the existing Claude subscription, and needs no GitHub App or model API key.
**Confirmation:** @anthropic-ai/claude-code and Promptfoo are exact development dependencies; The provider invokes the repository-local Claude binary with; Tests reject tool calls, additional turns, a missing or incorrect main model,; Tests prove the OAuth token reaches only the Claude child and never examples,; Every example retains three answer trials, three judge trials, deterministic; The GitHub job has only contents: read, retains redacted evidence for 14; Copilot permissions, credentials, packages, commands, and provider code are absent.; A real GitHub run using the subscription token passes before the release risk

### ADR-0026 — Example-Owned Quality Assurance Surfaces
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Example-owned quality commands with shared tooling"**, because it makes quality visible where adopters learn while keeping credentials, provider isolation, and exact-release evidence in one place.
**Confirmation:** Every runnable examples/*/package.json exposes test, test:built, and; Every runnable example contains at least one deterministic test and one; npm test -w <example workspace> succeeds from a clean installed checkout.; npm run test:llm -w <example workspace> runs only that example's live MCP; Root npm test builds once and runs every example-owned deterministic test; tests/docs/example-quality.test.mjs fails when a runnable example omits its; Native and React examples run browser, keyboard, and accessibility checks

### ADR-0027 — Public Semantic Testing Package
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0019"]
**Chosen:** Chosen option: **"One public `@emseepea/testing` package"**, because semantic qualification is a product capability and its deterministic and model-backed checks share the same server lifecycle and official MCP client boundary.
**Confirmation:** @emseepea/testing is public under MIT with no dependency on; Every runnable example imports the package as a development dependency and; No central map repeats example server paths, operations, or expected MCP; One example command runs only that example's deterministic checks; one runs; Semantic checks collect data through the official MCP client.; They ask the model three times and judge each answer three times.; They fail when required evidence is missing, the answer misses critical; They fail if the model uses tools, takes extra turns, or retries.; Package tests cover tool, resource, prompt, progress, authentication,; Clean packed installs and public smoke tests pass on supported Node versions.; Changesets and the existing release workflow publish both approved packages; The release record shows which commit was tested and published.; The root, examples, React package, and Tailwind package remain unpublished.

### ADR-0029 — Code-First Semantic Tests
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0024", "ADR-0026", "ADR-0027"]
**Chosen:** Chosen option: **"Direct code-first semantic tests"**, because normal code gives examples the needed flexibility. The existing runner already produces repeated answers and release evidence without Promptfoo.
**Confirmation:** @emseepea/testing has no Promptfoo dependency and passes a clean; Every runnable example owns an executable semantic test and exposes; A copied example runs its ordinary, lint, and semantic checks using packed; Tests demonstrate setup, cleanup, generated cases, several MCP operations,; Semantic tests use the official MCP client and record request and response; Each case produces three fresh answers and nine independent judgment records,; Tests reject missing evidence, failed critical facts, incorrect meaning,; Claude credentials reach only the isolated model process and never the; The exact release revision passes all semantic cases before npm publication.; The clean packed-install example gate finishes without installing Promptfoo.

### ADR-0030 — Public POST Progress Behind a Trusted Proxy
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0002"]
**Chosen:** Chosen option: **"Public POST progress through a trusted proxy"**, because a POST-scoped stream does not require shared state. A reverse proxy can select an instance and that instance can own the response until it ends.
**Confirmation:** Configuration rejects production progress for tools that require sign-in.; Black-box tests start a real reverse proxy and two independent Em See Pea; The proxy selects instances across requests while one instance owns each; Concurrent public calls receive their own ordered progress events and final; The proxy receives the no-buffering response instruction and forwards the; Invalid forwarding data is rejected before any tool handler runs.; Client disconnection cancels work in the selected instance.; Event count, event size, final response size, deadline, and cancellation; The proxy load scenario runs in the pull-request and exact-release workflows.; The scenario runs on Node.js 22 and 24.; Every request completes correctly and memory stays within the limit printed; The job fails if an event appears in the wrong request stream, is missing,; The existing performance limits for ordinary JSON requests remain unchanged; Public documentation describes only POST-scoped public progress and passes; No session, replay, shared-rate-limit, signed-in-streaming, GET-streaming, or

### ADR-0035 — Verified Guides Before Website Publication
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Executable guide checks"**, because readers need commands proven against installable packages.
**Confirmation:** Build and guide checks pass on clean Node.js 22 and 24 checkouts.; Routes, links, fragments, assets, canonical URLs, and sitemap checks pass.; Every runnable snippet is extracted and tested or links to tested source.; Every page passes automated accessibility checks in light and dark themes.; Named manual evidence covers keyboard use, focus, screen-reader output, 320 CSS pixel reflow, 400% zoom, force...; Every changed public page receives cognitive-accessibility review.; Published guide tests use the exact npm versions named to readers.

### ADR-0039 — Website Performance Budget
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Adopt the measured-build limits"**, because the first build passes them with room for variation.
**Confirmation:** Preserve the approved limits when checking a build for publication.; Bind the report to the exact Git revision and built-file hashes being published.; Check every file category and every phase in every required trial.; Reject missing, invalid, non-finite, or failed measurements and incomplete trials.; Require all numerical limits to pass; preserve failed results for diagnosis.; Keep guide and accessibility checks required by the other website decisions.

### ADR-0040 — Model-Selected Tool Semantic Tests
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0029"]
**Chosen:** Chosen option: **"Validated model-selected call plans"**, because it tests the model's choice while keeping execution, validation, credentials, cancellation, and evidence under one deterministic harness.
**Confirmation:** Deterministic tests accept correct advertised selections and reject missing,; Arguments must be objects and are checked again by the real MCP server.; Selected calls are executed only through the instrumented official MCP client.; Protected-tool tests prove the server receives its test token while model; Cancellation stops selection, MCP execution, answer, and judgment work.; Evidence records selection and operation hashes without raw private material.; Every tool-based example uses toolSelectionTest; the resource/prompt example; Copied examples pass lint, ordinary tests, and semantic smoke checks using; The exact publishing revision passes all real-model semantic cases before npm

### ADR-0042 — Separate Example Initializer Packages
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"One initializer package per example"**, because each command states the project being created and maps directly to npm's scoped initializer convention.
**Confirmation:** Each documented npm init @emseepea/<name>@next -- <directory> command creates; Every command refuses path traversal, a non-empty destination, and overwriting.; Generated projects are private and contain no workspace, root-relative,; Every generated project installs, lints, builds, runs ordinary tests, and runs; HTML and React projects retain their browser, keyboard, and accessibility tests.; Existing examples/* directories remain the only maintained template sources.; Changesets and the release workflow verify each initializer's package contents,; The repository uses one canonical list of public release packages.; Public guides and package READMEs explain the commands in plain language and

### ADR-0043 — Single Full Initializer Qualification Per Continuous Integration Event
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["0035-verified-guides-before-website-publication"]
**Chosen:** Chosen option: **"Fast Node matrix plus one full Node 24 initializer job"**, because supported-runtime compatibility and standalone package qualification are different evidence and do not need the same matrix.
**Confirmation:** Pull-request and main-branch Node.js matrix jobs run on Node.js 22 and 24 without the full eight-initializer t...; One Node.js 24 job per CI event runs the unchanged full initializer test against the canonical package list.; Website and package publication explicitly depend on their dedicated initializer jobs succeeding.; The full initializer test still checks generation, standalone dependencies, installation, lint, ordinary tests...; Live Claude semantic evaluation runs once per publication revision.; Post-publication verification still checks the actual registry packages and documented npm initializer command...; Routes, links, fragments, assets, canonical URLs, and sitemap checks pass.; Every runnable snippet is tested or links to tested source.; Automated accessibility checks pass in light and dark themes.; Named manual accessibility evidence and cognitive-accessibility review remain required.

### ADR-0045 — Quality-Gated Exact-Commit Release Continuation
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["0043-single-full-initializer-qualification-per-ci-event"]
**Chosen:** Chosen option: **"Quality-gated release continuation"**, because Quality can remain the unprivileged trust gate for untrusted `main`, while Release can reuse its exact-SHA evidence and retain only checks that must occur at release time.
**Confirmation:** Quality runs on pushes to main and retains Node.js 22 and 24, audit, performance, load, accessibility, website...; Release triggers only from a successful Quality workflow_run whose event is push, branch is main, repository i...; Every Release checkout and evidence-producing command uses the upstream Quality head SHA.; Release evidence records the upstream Quality run URL and exact SHA.; Live Claude evaluation runs once in Release before publication.; Release does not repeat the Node compatibility matrix or prepublication initializer qualification.; Eight registry initializer journeys run with four bounded workers and propagate every failure.; Audit retry and timeout bounds remain fail closed.; benchmark:built runs only after the same job successfully builds and tests the revision.; Publication still requires exact-SHA provenance, registry readback, SBOMs, signatures, clean installation, sem...; npm run push:watch waits for Quality before discovering Release and fails promptly when Quality fails.

### ADR-0046 — Lockfile-Constrained Dependency Verification
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0045"]
**Chosen:** Chosen option: **"Lockfile-constrained verification"**, because the committed lockfile and existing packed-consumer checks provide deterministic dependency evidence without depending on the unavailable advisory service. Published packages s...
**Confirmation:** Quality contains no npm vulnerability advisory call.; Release contains no npm vulnerability advisory call.; Packed consumers must use third-party versions present in the committed root lockfile.; Registry verification retains integrity, provenance, signature, and clean-install checks.; Quality and Release remain bound to the same exact commit.

### ADR-0051 — Latest as the Default Public npm Channel
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0042"]
**Chosen:** Chosen option: **"Publish all public packages to `latest`"**, because the qualified public releases are the versions Em See Pea recommends people use. Package maturity is communicated by `0.x` versions and clear pre-alpha wording, not by re...
**Confirmation:** The root release command publishes without a non-default tag override.; Public package manifests do not set publishConfig.tag to next.; Current README and website initializer commands omit @next.; Registry verification requires each released version to be the latest tag; All public packages are versioned and pass the existing exact-commit release; Every example directory name matches its @emseepea/create-* package suffix.; Each example remains the single maintained source for its initializer.

### ADR-0053 — Conversation-Style Semantic Tests
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0040"]
**Chosen:** Chosen option: **"Conversation-style JavaScript tests"**, because it makes the test's behaviour readable while reusing the qualified implementation already in `@emseepea/testing`.
**Confirmation:** Public types and deterministic tests cover context omission, sequential; Tool calls execute only through the instrumented official MCP client after; Literal containment tests reject non-string expectations and do not normalize; Meaning assertions use the existing strict judge verdict and retain three; Provider processes receive no server URL, server credential, or extra tool; Evidence binds each prompt, selection, MCP operation, answer, and judgment to; Every maintained example has readable conversation-style semantic tests.; Every packed initializer passes its semantic smoke check outside the; The exact publishing commit passes the live semantic job and all existing

### ADR-0054 — Provider-Native MCP Semantic Conversations
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0053"]
**Chosen:** Chosen option: **"Provider-native MCP conversations"**, because it tests the behaviour the framework claims to qualify without teaching the model how to select or format a call.
**Confirmation:** A behavioural test proves the model input equals the user's message exactly.; No structured-output schema, selection instruction, call-plan shape, answer; Exactly one loopback MCP server is configured and only its advertised tools; Tool assertions are populated only from native provider tool-use events.; A follow-up message uses the same provider conversation and can correctly; Unknown, unrelated, repeated, or more than three calls fail closed.; Shell, filesystem, browser, tool search, plugins, ambient settings, and; Credentials do not appear in prompts, provider output, failures, or evidence.; Prepared interpretation tests are labelled as such and make no native; Every maintained tool example passes live semantic qualification on the exact

### ADR-0055 — Native Client Journeys Only in Semantic Tests
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0054"]
**Superseded-by:** ["ADR-0057"]
**Chosen:** Chosen option: **"Native client journeys only"**, because semantic evidence must represent what users of the MCP will actually experience.
**Confirmation:** The public testing API has no prepare() method.; Semantic provider input contains only the exact user messages plus explicit; No harness-collected MCP material is sent to the conversation model.; Every semantic tool assertion comes from native provider tool-use events.; Documentation clearly says resources and prompts have deterministic protocol; Tests fail if prepare() or synthetic selection guidance is reintroduced.

### ADR-0056 — PostgreSQL-Backed Multi-Instance Initializer
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0051"]
**Chosen:** Chosen option: **"Replace SQLite with PostgreSQL"**, because a multi-instance example must preserve its coordination guarantees when instances run on different computers.
**Confirmation:** The active package list contains exactly eight initializers and replaces the; The new initializer creates a private standalone project from its maintained; Docker Compose starts PostgreSQL, and npm start starts two independent; A test races the same request through both instances and proves one stored; Tests prove database unavailability produces /readyz 503, generic tool; The example does not claim exactly-once external side effects, latency, or; The old creator is removed from npm, or deprecated with a pointer to the; Root guidance, example guidance, the documentation website, semantic tests,; The new package passes pack, standalone install, lint, ordinary tests,

### ADR-0061 — MongoDB JSON Schema Generated Internal Validation
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"MongoDB JSON Schema with Ajv and json-schema-to-ts"**, because MongoDB can enforce the schema and the application can reuse the same contract without introducing an object-document mapper.
**Confirmation:** Collection setup applies the identical as const schema object used by Ajv; A compatibility test proves every schema keyword used is accepted by MongoDB,; Tests prove MongoDB rejects invalid writes, application validation rejects an; The public MCP schemas contain descriptions and do not expose MongoDB IDs or; Tests prove fixed projections, bounded results and timeouts, generic errors,; Measured process CPU, memory, and network evidence is recorded before release.; The initializer creates a private standalone project and passes ordinary,

### ADR-0070 — OpenAPI-Generated Backend Types and Runtime Validation
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Canonical OpenAPI 3 with generated TypeScript and Zod"**, because one local contract can govern both compile-time and runtime backend checks while Swagger 2 remains an import format rather than a second internal model.
**Confirmation:** One committed canonical OpenAPI 3 document drives generated TypeScript and; A committed Swagger 2 fixture normalizes deterministically during the explicit; Clean offline regeneration produces no diff. Local fragment $ref values are; Changing a required field, optional field, or primitive category changes the; Mapper inputs and outputs compile against generated types without casts,; An invalid mapped backend request causes zero HTTP calls. An invalid provider; Public MCP schemas remain separately hand-authored, described, bounded, and; A structurally valid new provider string value in an approved public field; Callers cannot select the backend origin, operation, credentials, redirects,; The pinned generator, upgrader, and YAML parser pass licence, lockfile; Qualification records runtime-validator CPU and transient memory cost before

### ADR-0083 — Canonical Accessible Tool Result Views and MCP Apps Lifecycle
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Canonical result view, native and React renderers, and a standards-first lifecycle hook"**.
**Confirmation:** Strict parsing rejects unknown, oversized, inconsistent, and authority-bearing; Native and React renderers pass shared fixtures for every result state.; Hostile text cannot execute markup in either renderer.; Embedded renderers emit no document shell, main landmark, or H1.; Labelled values, lists, disclosure, disclaimer, and buttons retain native; The polite live region exists before updates and remains the same React node.; Keyboard, focus, label-in-name, axe, forced-colors, reduced-motion, 320-pixel; The lifecycle hook completes initialization before accepting tool results,; The published packages and initializer pass fresh-install verification.; A cited adopter replaces its generic result-presentation and host-lifecycle

### ADR-0088 — Always-Available Checked Model Context Protocol (MCP) Ping
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"Always admit the official MCP ping handler"**, because one framework admission change exposes behavior already implemented by the pinned official dependency without creating another public abstraction.
**Confirmation:** A raw MCP 2026-07-28 ping request returns the official handler's checked empty; Two independent clients using the official package pinned to MCP 2026-07-28; Ping succeeds when an application registers no tools, resources, or prompts.; Protected-discovery configuration causes zero verifier, authorization, and; Invalid origin, protocol version, Accept, content type, custom headers,; Ping emits no progress, logging, or cache activity and mutates no application; Explicit qualification records the unchanged behavior of every supported; No deprecated or speculative protocol capability is added.; Source, black-box, packed-package, and measured MCP 2026-07-28 performance checks pass from; A released-package journey independently verifies MCP 2026-07-28 ping, zero; Registry readback independently verifies the released package version,; Protocol coverage and package guidance describe ping as a core request, not; Deprecated client-logging, roots, and sampling guidance remains absent.

### ADR-0092 — Framework-Owned Model Context Protocol App Resource Packaging
**Status:** superseded | **Oversight:** confirmed
**Chosen:** Chosen option: **"One composite server helper"**, because one existing framework boundary can remove the repeated security-sensitive assembly while leaving the application-specific work outside the framework.
**Confirmation:** Public type checks must accept public and protected definitions and reject mixed; Definition tests must reject a non-ui:// URI, malformed Content Security Policy; A black-box modern and legacy client check must observe one URI and the MCP Apps; The resource listing and read content must carry equivalent modern and legacy; A mixed-case inline </script fixture must not end the generated script; The maintained React example must use the helper and retain its browser,; The canonical website guide must document the inputs, trust boundary,; The @emseepea/server README must link to the canonical guide without; Changed public guidance must pass cognitive-accessibility and voice-and-tone; Package, packed-initializer, exact-commit continuous integration, publication,

### ADR-0093 — Framework-Owned Model Context Protocol App Resource Packaging
**Status:** superseded | **Oversight:** confirmed | **Supersedes:** ["ADR-0092"]
**Chosen:** Chosen option: **"One composite server helper"**, because one existing framework boundary can remove the repeated security-sensitive assembly while leaving the application-specific work outside the framework.
**Confirmation:** Public type checks must accept public and protected definitions and reject mixed; Definition tests must reject a non-ui:// URI, malformed Content Security Policy; A black-box modern and legacy client check must observe one URI and the MCP Apps; The resource listing and read content must carry equivalent modern and legacy; A mixed-case inline </script fixture must not end the generated script; The maintained React example must use the helper and retain its browser,; The canonical website guide must document the inputs, trust boundary,; The @emseepea/server README must link to the canonical guide without; Changed public guidance must pass cognitive-accessibility and voice-and-tone; Package, packed-initializer, exact-commit continuous integration, publication,
