---
"@emseepea/create-openapi-backed-server": patch
---

Add a separate initializer that generates backend TypeScript declarations and
Zod runtime validation from a local OpenAPI contract, including Swagger 2
conversion and generation-drift checks.
