---
"@emseepea/react": minor
"@emseepea/create-react-ui-server": patch
---

Add a host-aware React theme hook with a reactive browser preference fallback,
and update the React UI initializer to apply the resolved theme in the app.
