---
"@emseepea/server": patch
---

Keep type inference responsive when `defineTool`, `defineStreamingTool`, or `defineMappedTool` use large Zod output schemas. Handler output checks remain unchanged.
