---
"@emseepea/server": patch
---

Add mapped tools with independently validated backend commands and results, one adapter call inside the shared deadline and cancellation lifecycle, and synthetic memory and file examples.
