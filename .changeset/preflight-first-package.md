---
"@emseepea/create-openapi-backed-server": patch
---

Publishing now stops before `npm publish` when a package name is absent from the registry and explains the separately authorized first-package setup.
